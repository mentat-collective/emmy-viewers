goog.provide('mathbox.primitives.operator.slice');
var module$node_modules$mathbox_react$build$cjs$index=shadow.js.require("module$node_modules$mathbox_react$build$cjs$index", {});
/**
 * *Select one or more rows/columns/stacks*
 * 
 * :classes `[]` (string array) - Custom classes, e.g. `[\"big\"]`
 * :depth `null` (nullable vec2) - Slice from, to depth (excluding to), e.g. `[2, 4]`
 * :height `null` (nullable vec2) - Slice from, to height (excluding to), e.g. `[2, 4]`
 * :id `null` (nullable string) - Unique ID, e.g. `\"sampler\"`
 * :items `null` (nullable vec2) - Slice from, to items (excluding to), e.g. `[2, 4]`
 * :source `\"<\"` (select) - Input source
 * y:width `null` (nullable vec2) - Slice from, to width (excluding to), e.g. `[2, 4]`
 */
mathbox.primitives.operator.slice.Slice = reagent.core.adapt_react_class(module$node_modules$mathbox_react$build$cjs$index.Slice);

//# sourceMappingURL=mathbox.primitives.operator.slice.js.map
