goog.provide('mathbox.primitives.time.clock');
var module$node_modules$mathbox_react$build$cjs$index=shadow.js.require("module$node_modules$mathbox_react$build$cjs$index", {});
/**
 * *Relative clock that starts from zero.*
 * 
 * :classes `[]` (string array) - Custom classes, e.g. `[\"big\"]`
 * :delay `0` (number) - Play delay
 * :from `0` (number) - Play from
 * :id `null` (nullable string) - Unique ID, e.g. `\"sampler\"`
 * :loop `false` (bool) - Loop
 * :pace `1` (number) - Play pace
 * :realtime `false` (bool) - Run on real time, not clock time
 * :seek `null` (nullable number) - Seek to time, e.g. `4`
 * :speed `1` (number) - Play speed
 * :to `Infinity` (number) - Play until
 */
mathbox.primitives.time.clock.Clock = reagent.core.adapt_react_class(module$node_modules$mathbox_react$build$cjs$index.Clock);

//# sourceMappingURL=mathbox.primitives.time.clock.js.map
